<!-- shows the post that is  -->
<h1 class="title">
 <%= @post.title %>
</h1>

<p>
	submitted <%= time_ago_in_words(@post.created_at) %>
  <%= link_to 'edit', edit_post_path(@post) %>
  <%= link_to 'delete', post_path(@post), method: :delete, data:{ confirm: 'are you sure?' } %>
</p>

<p>
	<%= @post.content %>
</p>